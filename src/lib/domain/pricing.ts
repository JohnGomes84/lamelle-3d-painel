import { money } from "./money";
export type PricingInput={grams:number;printMinutes:number;workMinutes:number;supplies:number;packaging:number;multiplier:number};
export type PricingSettings={filamentKgPrice:number;kwhRate:number;printerValue:number;printerLifeHours:number;laborHour:number;defaultMultiplier:number};
export function priceProduct(p:PricingInput,s:PricingSettings){const filament=p.grams/1000*s.filamentKgPrice,hours=p.printMinutes/60,energy=hours*.15*s.kwhRate,depreciation=hours*(s.printerValue/(s.printerLifeHours||2000)),failureReserve=(filament+energy+depreciation)*.1,labor=p.workMinutes/60*s.laborHour,totalCost=filament+energy+depreciation+failureReserve+labor+p.supplies+p.packaging,multiplier=p.multiplier||s.defaultMultiplier||3,salePrice=totalCost*multiplier;return{filament:money(filament),energy:money(energy),depreciation:money(depreciation),failureReserve:money(failureReserve),labor:money(labor),totalCost:money(totalCost),salePrice:money(salePrice),margin:totalCost?money((1-totalCost/salePrice)*100):0,belowFloor:multiplier<2.5}}
