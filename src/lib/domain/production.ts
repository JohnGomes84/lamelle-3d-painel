export type ProductionJob={id:string;kind:"order"|"portfolio"|"stock";eventDate?:string|null;depositSatisfied:boolean;manualPosition?:number|null};
export function sortProductionJobs<T extends ProductionJob>(jobs:T[]){const rank=(j:T)=>j.manualPosition!=null?-1:j.kind==="order"?(j.depositSatisfied?0:1):j.kind==="portfolio"?2:3;return [...jobs].sort((a,b)=>{if(a.manualPosition!=null||b.manualPosition!=null)return (a.manualPosition??Number.MAX_SAFE_INTEGER)-(b.manualPosition??Number.MAX_SAFE_INTEGER);return rank(a)-rank(b)||String(a.eventDate||"9999-12-31").localeCompare(String(b.eventDate||"9999-12-31"))})}
