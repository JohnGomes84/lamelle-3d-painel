export function toCsv(rows:Record<string,unknown>[]){const columns=[...new Set(rows.flatMap(Object.keys))],cell=(value:unknown)=>{const text=typeof value==="object"&&value!==null?JSON.stringify(value):String(value??"");return /[",\r\n]/.test(text)?`"${text.replaceAll('"','""')}"`:text};return "\uFEFF"+[columns.join(","),...rows.map(row=>columns.map(c=>cell(row[c])).join(","))].join("\r\n")}
