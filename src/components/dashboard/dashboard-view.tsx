const brl=(v:number)=>v.toLocaleString("pt-BR",{style:"currency",currency:"BRL"});
export function DashboardView({income=0,net=0,receivables=0,openOrders=0,lowStock=0,capacity=0}:{income?:number;net?:number;receivables?:number;openOrders?:number;lowStock?:number;capacity?:number}){const kpis=[[brl(income),"Entradas"],[brl(net),"Resultado de caixa"],[brl(receivables),"A receber"],[String(openOrders),"Pedidos abertos"],[String(lowStock),"Estoques no mínimo"]];return <><section className="page-heading"><span className="eyebrow">Visão geral</span><h1>O negócio, neste mês</h1><p>Receita, compromissos e capacidade de produção em uma única leitura.</p></section><section className="kpi-grid" aria-label="Indicadores do mês">{kpis.map(([v,l])=><article className="kpi-card" key={l}><strong>{v}</strong><span>{l}</span></article>)}</section><section className="dashboard-grid"><article className="panel"><h2>Próximas entregas</h2><p className="muted">{openOrders?`${openOrders} pedido(s) em andamento.`:"Nenhum pedido aberto."}</p></article><article className="panel"><h2>Ocupação semanal</h2><strong className="large-number">{capacity.toLocaleString("pt-BR",{maximumFractionDigits:1})}h</strong><div className="capacity-bar"><i style={{width:`${Math.min(100,capacity/40*100)}%`}}/></div><p className="muted">Bambu Lab A1 · 40h disponíveis</p></article></section></>}
