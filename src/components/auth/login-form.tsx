"use client";
import { useActionState } from "react";import { requestLogin,signInWithPassword,type LoginState } from "@/app/(auth)/login/actions";
const initial:LoginState={};
function MagicLinkForm(){const[state,action,pending]=useActionState(requestLogin,initial);return <details className="login-recovery"><summary>Não consegue entrar? Receber link por e-mail</summary><form action={action} className="login-form compact-form"><label htmlFor="recovery-email">E-mail autorizado</label><input id="recovery-email" name="email" type="email" autoComplete="email" required/><button className="secondary-button" disabled={pending}>{pending?"Enviando…":"Enviar link de recuperação"}</button>{state.error&&<p className="form-error">{state.error}</p>}{state.success&&<p className="form-success">{state.success}</p>}</form></details>}
export function LoginForm(){const[state,action,pending]=useActionState(signInWithPassword,initial);return <><form action={action} className="login-form"><label htmlFor="email">E-mail</label><input id="email" name="email" type="email" autoComplete="email" required placeholder="voce@exemplo.com"/><label htmlFor="password">Senha</label><input id="password" name="password" type="password" autoComplete="current-password" required/><button disabled={pending}>{pending?"Entrando…":"Entrar"}</button>{state.error&&<p className="form-error">{state.error}</p>}</form><MagicLinkForm/></>}
