"use client";
import { useState } from "react";import { createOrder } from "@/app/(protected)/orders/actions";
type Option={id:string;name:string};
export function OrderForm({clients,products}:{clients:Option[];products:Option[]}){const[lines,setLines]=useState([0]);return <details className="create-box order-create"><summary>+ Novo pedido</summary><form action={createOrder} className="entity-form"><label><span>Cliente</span><select name="client_id" required>{clients.map(c=><option value={c.id} key={c.id}>{c.name}</option>)}</select></label><label><span>Data do evento</span><input name="event_date" type="date"/></label><label><span>Data de entrega</span><input name="delivery_date" type="date"/></label><label><span>Observações</span><textarea name="notes"/></label><div className="order-lines">{lines.map((key,index)=><div className="order-line" key={key}><label><span>Produto</span><select name="product_id" required>{products.map(p=><option value={p.id} key={p.id}>{p.name}</option>)}</select></label><label><span>Quantidade</span><input name="quantity" type="number" min="1" defaultValue="1" required/></label><label><span>Personalização individual</span><select name="individualized"><option value="false">Não</option><option value="true">Sim</option></select></label>{lines.length>1&&<button type="button" onClick={()=>setLines(x=>x.filter((_,i)=>i!==index))}>Remover</button>}</div>)}</div><button type="button" className="secondary-button" onClick={()=>setLines(x=>[...x,Date.now()])}>+ Adicionar item</button><button disabled={!clients.length||!products.length}>Salvar pedido</button></form></details>}
