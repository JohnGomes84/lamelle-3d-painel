"use server";
import { z } from "zod";import { headers } from "next/headers";import { createClient } from "@/lib/supabase/server";
export type LoginState={error?:string;success?:string};const Email=z.string().trim().toLowerCase().email("Informe um e-mail válido.");
export async function requestLogin(_:LoginState,formData:FormData):Promise<LoginState>{const parsed=Email.safeParse(formData.get("email"));if(!parsed.success)return{error:parsed.error.issues[0].message};const allowed=[process.env.LAMELLE_OWNER_EMAIL,process.env.LAMELLE_PARTNER_EMAIL].filter(Boolean).map(x=>x!.toLowerCase());if(allowed.length&&!allowed.includes(parsed.data))return{error:"Este e-mail não foi convidado para a Lamelle 3D."};const origin=(await headers()).get("origin")||process.env.NEXT_PUBLIC_APP_URL||"http://localhost:3000";const supabase=await createClient();const{error}=await supabase.auth.signInWithOtp({email:parsed.data,options:{emailRedirectTo:`${origin}/auth/callback`}});return error?{error:"Não foi possível enviar o acesso. Tente novamente."}:{success:"Enviamos um link seguro para o seu e-mail."}}
